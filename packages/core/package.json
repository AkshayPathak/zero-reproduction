{
  "name": "@new-zero/core",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "test": "sst shell vitest",
    "generate": "sst shell drizzle-kit generate",
    "migrate": "sst shell drizzle-kit migrate",
    "studio": "sst shell drizzle-kit studio",
    "seed": "sst shell bun run src/util/seed.ts"
  },
  "exports": {
    "./*": [
      "./src/*/index.ts",
      "./src/*.ts"
    ]
  },
  "dependencies": {
    "drizzle-orm": "^0.43.1",
    "drizzle-seed": "^0.3.1",
    "drizzle-zod": "^0.7.1",
    "postgres": "^3.4.5",
    "sst": "*",
    "ulidx": "^2.4.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.1",
    "vitest": "^2"
  }
}
